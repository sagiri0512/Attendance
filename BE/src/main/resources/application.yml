server:
  servlet:
    context-path: /api

spring:
  application:
    name: BE
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/attendance?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: sagiri
    password: kong123456

logging:
  level:
    com.sagiri.mapper: DEBUG

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl